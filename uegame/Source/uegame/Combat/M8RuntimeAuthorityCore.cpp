// M8RuntimeAuthorityCore.cpp - compile the shared engine-independent runtime policy
// into the UE runtime module exactly once. The source remains at the repository root so
// the same bytes are exercised by the standalone CTest target.

#include "m8_runtime_authority.cpp"
