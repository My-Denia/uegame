// M8RoomContractCore.cpp - compile the shared engine-independent room-contract
// transaction policy into the UE runtime module exactly once. CTest exercises the
// same repository-root source bytes independently of Unreal Engine.

#include "m8_room_contract.cpp"
